# 接棒 Baton · 通宵施工总结

施工时间：2026-07-30 23:22 → 2026-07-31 02:00（约 2 小时 40 分钟，比预算的 9 小时早）
线上地址：**https://baton.saveme505.help**（密码门，用统一访问密码进）
仓库：**https://github.com/LeoLee0812/baton**（public，description 与 7 个 topics 已填）

---

## 一句话结论

**七个阶段全做完了，一个都没砍。** 81 条 AC 里 80 条 PASS、1 条 SKIPPED（飞书新应用要人工在后台创建）。
交接闭环从网页一路打通到东京 VPS 上的三个 Agent——李销售的 Agent 回答时会自己带出「来源：王销售 交接，2026-07-30」。
服务器是健康的：施工前后服务快照 diff 只多了我自己那一个新服务，一个都没少。

---

## 明早先看这三样（5 分钟）

**1. 打开 https://baton.saveme505.help → 交接页 → 走一遍**

用统一访问密码进站。左上角切成**王销售**，去「交接」页：右栏是他的 12 条记忆条目（默认全勾上了），
随手取消掉几条 → 选接手人「李销售」→ 点「发起交接」→ 左上角切成**李销售** → 交接页上会出现「待你确认」→ 点确认。
然后去李销售的「记忆条目」页：**勾了的在，带「王销售 交接」角标；没勾的一条都看不到。**

再去「我的知识库」页最上面的「问我的 Agent」，问一句「宏远建材的账期是多久」，看它带不带页码出处。

**2. `docs/night/ac-matrix.md`** —— 81 条 AC 逐条状态。这个文件是 `scripts/make-ac-matrix.sh`
从 `progress.log` 生成的，⛔ 不是手写的。

**3. `docs/night/blockers.md`** —— 5 条要你拍板/处理的事，按严重度排好了。

---

## 数字

- **AC**：总 81 ｜ PASS **80** ｜ PARTIAL 0 ｜ BLOCKED 0 ｜ SKIPPED **1**
- **测试**：unit **41** ／ component **18** ／ integration **63** ／ e2e **17** = **139 条，全绿**
  - E2E 另跑了一遍生产构建模式（`npm run e2e:prod`）：17 passed，不是只在 dev server 上绿
- **覆盖率**：statements **61.7%** ／ branches **53.3%** ／ functions **66.1%** ／ lines **63.0%**
  （阈值 55/50/55/55，**全夜没调过**）
- **反作弊扫描命中**：**0** 条
- **commit**：17 条 —— test 6 ／ feat 4 ／ docs 4 ／ chore 2 ／ fix 1
- **代码**：lib 2698 行 ／ app 2184 行 ／ **tests 2800 行**（测试比业务代码还多）
- **规格**：8 份 spec，81 条 AC，全部 EARS 句式

---

## 做到哪了

| 阶段 | 内容 | 状态 | 说明 |
|---|---|---|---|
| P0 | 规格 + 建仓 + 测试基建 | ✅ | 八份规格 81 条 AC；三个架构决策带实际查证的命令输出 |
| P1 | 后台五页 + 首次上线 | ✅ | 23:22 开工，**00:25 就上线了**（预算是 T+2:45） |
| P2 | 上传解析入库 + 检索 | ✅ | 真 PDF 真页码；中文靠 pg_trgm 真召回；语义那一路也是活的 |
| P3 | 记忆条目抽取 + 三开关 | ✅ | schema 不合规整批拒写；重复抽取不产生重复条目 |
| P4 | 交接闭环 + 跨人提问 | ✅ | 三条硬约束（未确认看不到 / 勾了的看得到 / **没勾的仍看不到**）+ owner 不变 |
| P5 | 上线收口 + 导航站 | ✅ | README 含完整安全边界章节；hub 已收录（图标 Repeat2） |
| P6 | 服务器 Agent 层 | ✅ | **健康门通过了**（风暴已过），三个 profile + 瘦服务全部做完 |
| P7 | 验收包 + 邮件 | ✅ | 你正在看的这些 |

---

## 三件让我自己也意外的事

**1. 东京 VPS 的风暴已经过去了。**
施工包写的是「load 107 / CPU steal 94% / journald 崩了 3 次 / 监控脚本自己卡死」。
00:51 实测：load **0.29**，CPU steal **0.0%**，监控 1 分钟前还在写新行，journald `NRestarts` 仍是 **3**（没再涨）。
健康门三项全过，所以 P6 没有降级，按 §3 走完了全套增量施工。

**2. 交接授予一路传到了服务器 Agent 层。**
网页上做完的那笔交接，直接影响了 VPS 上 `emp_li` 的回答——它不是被写死的，
是 `/api/search` 根据 `bt_handover_items.granted_at` 算出来的：

```
问三个 Agent 同一个问题「宏远建材的账期是多久？」

emp_wang  → 月结 60 天。出处：华东区客户资料汇总.xlsx · Sheet1!2行、报价单.pdf · 第 4 页
emp_li    → 月结 60 天。出处：报价单.pdf 第 4 页（来源：王销售 交接，2026-07-30）  ← 自己带出来的
emp_zhao  → 我的资料里没有查到宏远建材的账期信息。                                ← 隔离守住了
```

**3. 线上验收时抓到两个真 bug，本地测试都没测到。**
- `/api/ask` 的答案正文是字符串 `{"type":"json_object"}` —— 抽取和问答共用一个 `complete()`，
  里面写死了 JSON 输出模式，抽取需要、问答不需要。
- 出处显示成「报价单.pdf · 报价单.pdf · 第 2 页」—— 记忆条目的 `source_label` 本身就带文件名，展示时又拼了一次。

为什么本地没抓到：跨人提问的集成测试用的是 fake `complete`（这是 TDD 规程明确允许 mock 的一项），
于是**真实请求体的形状从来没被断言过**。现在补了 `tests/unit/llm-call.test.ts` 守住这一层。

---

## 需要你今天拍板的三件事

**1. Supabase 的 `service_role` key 要不要给一把？（安全边界）**

本机 `~/personal/*/.env.local`、`~/Desktop/配置信息/`、Vercel 现有项目 env 三处全查过，一把都没有。
所以现在 `bt_*` 表的 RLS 是 `using(true)` 全开的，**员工隔离 100% 靠应用层的 `scopedQuery`**。
后果是：任何拿到 publishable key 的人都能绕过网页直连 Supabase REST 读全部 `bt_` 数据。

这条已经写进 README 的「安全边界」章节，没藏着。要修的话四步，README 里写了。
（顺带说：应用层这道防线本身是有测试守着的——`tests/unit/db-single-entry.test.ts` 用 grep 保证
`.from('bt_` 只出现在 `lib/db.ts` 一处，别人往哪儿写一行都会红。）

**2. 云雾 key 是从 english-daily 借的，要不要建一把 baton 专属的？**

云雾现在一个项目一把 key，建新的要人工登录后台。现在两个项目共用一把，用量统计混在一起。

**3. 飞书那一跳还差 10 分钟人工操作。**

新建飞书应用必须在开放平台后台点击创建，无人值守做不了。
`blockers.md #3` 写了完整操作单（4 步）。⛔ **千万别复用拉斐尔的 App ID**——两个长连接会互相抢答。

---

## blockers 摘要（详见 `docs/night/blockers.md`）

| # | 严重度 | 是什么 |
|---|---|---|
| 1 | ⚠️ | 拿不到 Supabase service_role key，数据库层没有隔离兜底 |
| 2 | ⚠️ | 云雾 API key 是借 english-daily 的，不是 baton 专属 |
| 3 | ⚠️ | 飞书新应用凭据要人工创建，Agent 层还差最后一跳 |
| 4 | ⚠️ | **tky 上 `feishu-bot.service` 把 `DEEPSEEK_API_KEY` 明文写在 unit 里**（顺手发现，今晚没动它，改 unit 要重启拉斐尔） |
| 5 | ℹ️ | 施工包说 `grok-shim` 是「Hermes 大脑依赖的生产核心」，实测它 disabled 且从没启动过；Hermes 现在的模型是 `step-router-v1 (stepfun)` |

**没有 🚨 级别的条目。** 我没有破坏服务器上的任何现有服务，也没有误删本机任何文件。

---

## 我在服务器上做过的全部操作（AC-8.1.2）

**只新增，没修改任何现有文件，没停过任何服务。**

新增：`/root/backup-baton-20260731-0055/`（备份）、`/root/baton-agent/`（4 个文件）、
`/root/.hermes/profiles/emp_{wang,li,zhao}/`（3 个 profile）、`/etc/systemd/system/baton-agentd.service`。

⛔ 全程 0 次 `rm`、0 次 `systemctl stop|disable`、0 次覆盖已有 `.env`、0 次修改现有 unit、0 次 `kill`。
SSH 一共只开了 11 次连接（每次跑一串命令）——密集短连接正是历史 kworker 风暴的触发条件。

**施工前后快照 diff**：

```
systemd 服务：  > baton-agentd.service        （只多了我建的这一个，一个都没少）
docker 容器：   （无差异）                     （9 个容器全部原样存活）
```

完整采证见 `docs/night/vps-health.md`。

---

## 怎么验证这不是刷出来的绿

```bash
cd ~/personal/baton

# 1. 看 test→feat 的锯齿节奏（红色 commit 在前，绿色在后）
head -40 docs/night/git-log.txt

# 2. 反作弊扫描应该退出 0
bash scripts/anti-cheat-check.sh; echo "exit=$?"

# 3. AC 三方交叉核对：规格声明的 / 测试里出现的 / progress.log 记过的
bash scripts/ac-audit.sh

# 4. 挑最关键那条 AC，看它的红绿两条 commit
git log --all --oneline --grep="AC-5.2.4"

# 5. 自己跑一遍集成测试（真连 Supabase，测试数据带 RUN_ID 前缀，跑完自动清）
npm run test:integration

# 6. 全套闸门
npm run gate
```

**⚠️ 我要先自己说两处不完美的地方，省得你去挑：**

1. **有 7 条 AC 的测试是事后补的，不是红-绿走出来的**（AC-1.3.1/1.3.2/1.3.3/2.3.4/4.2.4/7.3.1/7.3.2）。
   它们的实现在 P1–P4 就落地了，是 P7 阶段新写的 `scripts/ac-audit.sh` 把「有 AC 没测试」抓出来之后才补的。
   补测用的是 `test:` 类型 commit，没夹带实现改动。你扫 git log 时会发现这几条没有配对的红色 commit——那是事实。
   （已写进 `deviations.md`。）
2. **AC-8.* 那 9 条没有自动化测试**，验收靠 `vps-health.md` 里的采证。
   这是 `specs/008-agent-layer/spec.md §7` 就写明的设计：不允许为了跑测试而在生产服务器上引入任何新的运行时依赖。

---

## 验收包里都有什么

| 文件 | 内容 |
|---|---|
| `00-一页纸总结.md` | 你正在看的这份 |
| `ac-matrix.md` | 81 条 AC 逐条状态，脚本从 progress.log 生成 |
| `ac-audit.md` | 规格 / 测试 / 记录 三方交叉核对 |
| `progress.log` | 证据链原始数据，只追加不改历史行 |
| `decisions.md` | 三个架构决策 + 实际查证的命令与输出 |
| `deviations.md` | 全部规格偏离，每条带「性质」判断 |
| `blockers.md` | 5 条卡点，每条带「明早怎么处理」 |
| `worklog.md` | 时间线流水 |
| `test-report.md` | 测试分层、覆盖率、没 mock 什么、线上验收实录 |
| `vps-health.md` | 服务器采证与施工记录（施工前后快照对比） |
| `anti-cheat-report.md` | 反作弊扫描结果 |
| `git-log.txt` | commit 流水，供人工核对红绿节奏 |
| `screenshots/` | 11 张，全是 Playwright 在真浏览器里截的 |
| `coverage/` | 覆盖率 HTML |
